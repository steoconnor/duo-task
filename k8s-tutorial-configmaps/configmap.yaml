apiVersion: v1kind: ConfigMapmetadata:  name: nginx-configdata:  default.conf: | # in YAML syntax, | indicates a multi-line string    server {      listen 80;        location / {          return 200 "Hello from NGINX\n";        }    } 